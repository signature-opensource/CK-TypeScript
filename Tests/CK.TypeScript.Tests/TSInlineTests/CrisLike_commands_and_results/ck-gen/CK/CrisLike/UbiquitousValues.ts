import { IPoco } from '../Core/IPoco';
import { CTSType } from '../Core/CTSType';

/**
 * Defines an extensible set of properties that can be initialized from or need to
 * be challenged against the endpoint services.
 * 
 * The ~~!:IUbiquitousValuesCollectCommand~~ sent to the endpoint returns these values.
 **/
export class UbiquitousValues implements IPoco {
public constructor()
public constructor()
constructor()
{
CTSType["CK.CrisLike.IUbiquitousValues"].set( this );
}
readonly _brand!: IPoco["_brand"] & {"2":any};
}
